# slop-livin — founding session

## Aim
Developers running many coding agents keep enough free space to keep working, without emergency scans and repeated cleanup. Desired behavior: know what grew, by project, and what to do about it — where "do" is one of four verbs (delete, archive, prevent, leave), chosen ahead of an incident, and where the everyday operator is an AI agent acting on the developer's behalf with the developer authorizing anything destructive.

## Problem Space (carried from the Mole session, 2026-09-17)
- Unit of decision is the **project**, with **activity** as the deciding attribute. Per-artifact-type buckets were unanswerable; every answer was "depends whether the project is active."
- Four response verbs, not one: delete (build output of inactive projects), archive (whole inactive project), prevent (151 `node_modules` → "use pnpm"), leave (`.git`, always).
- Three grouping axes: artifact type × project × worktree (worktree nests in project; type cuts across).
- Docker is a separate accounting domain: ~24 GB inside a VM disk, invisible to any walk; attribution via labels (rare) or name heuristics; shared-vs-unique layers mean reclaim is set-valued.
- Volume truth: 360 GB used, index covers 200 GB, ~47 GB unattributed after all top-level dirs; 3 APFS local snapshots present → "bytes deleted" ≠ "space freed".
- Measured on one machine, one operator, one day, no disk pressure during observation (100 GB free). n=1; the zero-deletion signal is weak.

## Salvage (carried)
Plumbing that carries over from `muness/mole` branch `integrate`: parallel walk with folding and deterministic hardlink attribution; one-base + reverse-delta log (zstd-9/3); FSEvents cgo helper (flat ~0.13 s replay, ~16 h horizon); opt-in LaunchAgent refresh; device boundary; abandon-not-join for stalled reads; three AST audits. Known defects on import: store keyed per root (8 overlapping stores); `RepoRootID`/`LinkedWorktree` declared but never populated; TUI deletion doesn't invalidate; per-keypress rescan. The Mole surfaces (tree TUI, growth view) do not carry over.

Architecture reference: `~/src/open-horizon-labs/repo-native-alignment` — `Extractor` per source, `Enricher` async, event bus with consumers, provenance + confidence on every fact; guardrails `extract-fully-at-parse-time`, `computed-but-not-delivered`.

Competitive gap (researched 2026-09-17): kondo has the project model (marker files, `--older`) but is stateless, `rm -rf`, no Docker/worktrees. StorageRadar has the only growth history (manual directory snapshots, GUI, $19.99) and a read-only MCP endpoint for agents. Nothing combines project model × persistent history × Docker × worktrees × agent operation.

## Hard constraints
- Deletion always routes through protection / whitelist / dry-run / action-boundary checks with a live recheck at the sink. Index is evidence, never authorization. **An agent is also never authorization.**
- Git worktree staleness is not decidable → never a "safe to delete" verdict for a worktree; activity is a signal.
- Folded artifact trees are one unit of action each.
- Totals must match a live walk of what was scanned; coverage vs volume reported separately and honestly.
- Trash by default for user-facing removals.
- Rust. Single static binary; FSEvents via CoreServices FFI in-process (no separate helper). Decided 2026-09-18: fspulse scanner and reverse-delta design were Rust; repo-native-alignment (the architecture reference) is Rust; arrow-rs/parquet tooling is stronger. The Go plumbing on mole `integrate` is a *design* reference and test oracle, not a port source.
- No claiming a trial passed before it is observed.

## Problem Weave

**Current frame:** A developer running many coding agents wants free space to stay workable without emergency scans. The everyday operator is an agent; the human only authorizes. Four verbs (delete / archive / prevent / leave) applied to projects, with activity deciding.
**Layers used:** Agent as operator (A) — what the agent must be able to ask, propose, and be refused. Safety & human authorization (H) — what is structurally impossible and what consent looks like. Domain model & system (D) — what must be representable so decisions attach to durable things. Evidence & measurement (E) — how anyone would know the loop works and is safe.
**Independence:** four parallel agents, same contract, no cross-reading.
**Status:** candidate frames; one material opposition (recommendation vs verdict) needs the maintainer's call; the rest are sequencing or evidence.
**Inputs:** `## Aim`, `## Problem Space`, `## Salvage`, `## Hard constraints` above.

### Shared Core (every layer landed here independently)
1. **Decisions attach to entities, not paths.** Project identity from git's object store (survives rename/move/archive); artifact instance keyed by (project, kind, relative location) so delete-then-rebuild is *regrowth of one thing*. Path is an attribute. (D-2, D-4; A-4 and H-1 both depend on it.)
2. **Every fact carries source, confidence, and freshness.** Not advisory — a bare number is a defect because the agent will sum a walked byte with an inferred Docker byte and act on a 16-hour-old fact. (A-1, D-5, D-7, E-5, H-3)
3. **Proposer can never be authorizer.** The agent produces a frozen plan; execution accepts only a plan reference plus a *grant* the agent could not have minted. Grant = verb + predicate over tool-computed facts + scope + expiry + blast-radius budget. Re-derived at the sink from fresh facts; "could not re-observe" is a refusal. (A-3, A-4, H-1, H-2, H-5)
4. **Success = free space observed after, not bytes deleted.** Trash, APFS snapshots, and concurrent writers make the gap the normal case. The model holds three terms: attributed, volume-reported, named residual. (A-6, D-6, E-4)
5. **The ledger is not the index.** The index is a rebuildable cache; the ledger (verb, entity, evidence, grant, actor, observed effect) must outlive it. (D-10, H-7, E-4)
6. **Stopping is a success state with a named cause.** nothing-worth-doing / insufficient-evidence / index-stale / busy / claimed-by-another-agent / awaiting-authorization. An agent that can't tell them apart retries forever. (A-5, A-8, H-6)
7. **Absence is caught statically.** The two costliest prior defects (unpopulated identity columns, deletion never invalidating) were invisible to green tests. Extend source-invariant audits: every declared column has a writer, every identity produced is consumed, no sink reads identity from the index without live rebinding. (D-11)

### Distinct Problem Statements (kept separate on purpose)
1. **Agent:** an agent needs question-shaped answers with a recommended verb and a way to stop, because it has speed and no peripheral vision; today every surface is a size tree for a human.
2. **Safety:** a developer needs a consent object between "confirm each" and "let it clean," because per-action confirmation trains rubber-stamping and standing trust is a blank check; today no such object exists.
3. **Model:** the four verbs need a stable subject that survives rename, rebuild, and medium (Docker has no path), because path-keyed decisions get re-asked exactly when they matter.
4. **Evidence:** nobody can yet say whether the loop works; every number is instrument telemetry from one machine-day and the premise (pressure ever occurs; the agent raises decision throughput) is untested.

### Interwoven S&T Steps
| ID | Parent | Depth | Layer | Strategy | Tactic | Disp. | Links |
|---|---|---|---|---|---|---|---|
| D-1 | root | 1 | D | one authoritative state per volume | key store by volume; roots are observed regions | candidate | enables D-6; fixes 8-store defect |
| D-2 | root | 1 | D | project identity survives path changes | git object-store identity, path fallback at low confidence | candidate | required by A-4, H-1, D-10 |
| D-3 | D-2 | 2 | D | identity recoverable without un-folding | detect repo root at classify boundary, before fold | candidate | fixes unpopulated RepoRootID |
| D-4 | root | 1 | D | artifact identity independent of existence | key (project, kind, rel-path); tombstone/rehydrate | candidate | basis for `prevent` (regrowth) |
| D-5 | root | 1 | D | activity is several signals, not a score | mtime / git / live-system signals, each sourced | candidate | complements H-3; worktree undecidability |
| D-6 | root | 1 | D | deleted ≠ freed is sayable | three-term residual with typed causes | candidate | = A-6, E-4 |
| D-7 | root | 1 | D | fs, git, Docker feed one graph | extractor contract, pathless entities join by relationship | candidate | RNA pattern |
| D-8 | D-7 | 2 | D | Docker join honest about weakness | confidence-bearing edge; `image.source` → base image, never project | candidate | opposition with H G-C (Docker fact-only) |
| D-9 | root | 1 | D | recovery contract selects the verb | per-kind classification + restore-source relationship | candidate | resolves A-2 vs H-3 (see below) |
| D-10 | root | 1 | D | decisions are durable data | append-only ledger keyed by entity, outside the index | candidate | = H-7, E-4 |
| D-11 | root | 1 | D | absence enforced statically | extend AST audits to model wiring | candidate | RNA `computed-but-not-delivered` |
| A-1 | root | 1 | A | agent judges fact usability itself | provenance/confidence/freshness mandatory per fact | candidate | = E-5 |
| A-2 | root | 1 | A | agent asks loop-shaped questions | pressure → changed → worth-acting (ranked, verb) → unit+undo → did it work | candidate | **opposes H-3** |
| A-3 | root | 1 | A | proposal is a first-class object | frozen plan + token agent can't mint; expires; re-validated at sink | candidate | = H-1/H-5 |
| A-4 | root | 1 | A | never re-ask | three grains (action/policy/session) queryable *before* proposing | candidate | = H-1; depends on D-10 |
| A-5 | root | 1 | A | stopping is normal | typed terminal states with cause + next step | candidate | Mole refusal-diagnostics pattern |
| A-6 | root | 1 | A | verify the developer's goal | required post-action check in free-space terms | candidate | = D-6, E-4 |
| A-7 | A-1 | 2 | A | agent sees its own footprint | actor/session attribution of growth | candidate | conditional; overlaps H-8 |
| A-8 | A-5 | 2 | A | many agents don't collide | single-claim units, single-use plan refs | candidate | conditional on multi-agent use |
| H-1 | root | 1 | H | consent reusable without blank check | scoped, expiring, fact-conditioned grant | candidate | = A-3/A-4 |
| H-2 | H-1 | 2 | H | grants don't drift | blast-radius budget + first-match tripwire; renewal shows receipt | candidate | Mole #1459 shape |
| H-3 | root | 1 | H | no ungrounded verdicts | facts + confidence; forbid "safe/stale/unused"; unknown fails closed | candidate | **opposes A-2** |
| H-4 | root | 1 | H | per-verb authorization bar | leave < prevent(advice) < delete(Trash) < archive(partial-failure) | candidate | trade-off: archive above delete |
| H-5 | H-4 | 2 | H | authorization survives the gap | sink re-derives the grant predicate, not path existence | candidate | cost vs safety (E) |
| H-6 | H-4 | 2 | H | partial failure is a state | per-unit outcome record written durably before/after | candidate | ENOSPC mid-archive |
| H-7 | root | 1 | H | "what did it delete Tuesday and why" | audit independent of index, with actor + grant | candidate | = D-10 |
| H-8 | root | 1 | H | don't eat the operator | live agent occupancy is a non-overridable protection fact | candidate | **new; immediate** |
| H-9 | root | 1 | H | authorization can't be manufactured | disk content is never instruction; low space raises urgency not permission | candidate | prompt-injection surface |
| E-1 | root | 1 | E | one primary outcome variable | pressure episodes avoided/week; GB-that-stayed-freed secondary | candidate | demotes latency/size |
| E-2 | E-1 | 2 | E | baseline before agent acts | passive observation period using existing log | candidate | |
| E-3 | root | 1 | E | proposal quality is a rate | score authorized/declined/modified/ignored | candidate | = ground truth is a proxy |
| E-4 | root | 1 | E | ledger records effect not claim | observed post-state distinct from prediction | candidate | = D-6, A-6 |
| E-5 | root | 1 | E | detect stale action | staleness age + horizon flag per fact; horizon-exceeded is an event | candidate | = A-1 |
| E-6 | root | 1 | E | pre-registered case study | duration, floor, event minimum, three readings | candidate | not a "trial" |
| E-7 | root | 1 | E | tool's own I/O cost | contention vs concurrent agents as standing cost line | candidate | unmeasured |
| E-8 | root | 1 | E | n=1 stated everywhere | population + nearest machine where it fails | candidate | |

### Sufficiency Groups
| Group | Parent | Children | Mode | Coverage claim | Gap |
|---|---|---|---|---|---|
| SG-state | root | D-1, D-6 | all | one denominator + sayable residual | shared-pool APFS containers; purgeable space observability |
| SG-project | root | D-2, D-3 | all | identity defined and actually captured | non-git projects; merge/split semantics |
| SG-artifact | root | D-4, D-9 | all | subject + verb selector | cross-project shared caches (no single owner); per-instance overrides |
| SG-activity | root | D-5 | all | signals not verdicts | which signals are cheap enough at walk time |
| SG-sources | root | D-7, D-8 | all | pathless source passes the contract | Docker volumes vs images (different recovery contract); snapshots as entities |
| SG-durable | root | D-10, D-11 | all | decided stays decided; absence caught | ledger conflict semantics |
| SG-read | root | A-1, A-2, A-7 | A-1,A-2 req; A-7 cond | loop-shaped, self-describing answers | how the agent *notices* (poll vs push) |
| SG-act | root | A-3, A-4, H-1, H-2 | all | proposer ≠ authorizer; answer once | grant authoring path; revocation; where `prevent` lives |
| SG-stop | root | A-5, A-6, A-8 | A-5,A-6 req; A-8 cond | honest termination | what to do when the human never answers |
| SG-shown | root | H-3, H-4 | all | non-leading facts, proportional bars | reviewability of 272k dirs folded into projects |
| SG-exec | H-4 | H-5, H-6 | all | world changed / died halfway | Docker sink recheck unstatable → fact-only |
| SG-account | root | H-7, H-8, H-9 | all | answerable, self-preserving, unforgeable | shared/managed machines: explicit "not supported" |
| SG-works | root | E-1, E-2, E-6 | all | falsifiable efficacy claim | no counterfactual arm |
| SG-safe | root | E-3, E-4, E-5 | all | leading indicator, record, load-correlated failure | regret rate; near-miss counting |
| SG-cost | root | E-7, E-8 | all | numbers aren't free or universal | second machine |

### Overlap and Complement
- A-3/A-4 ≡ H-1/H-2 (merged as SG-act; wording kept: "token the agent can't mint" (A) vs "predicate over facts" (H) — both needed).
- A-6 ≡ D-6 ≡ E-4: same relation seen from operator, model, and measurement.
- D-10 ≡ H-7 ≡ E-4's ledger; D-10 adds "must live outside the index."
- A-1 ≡ E-5; D-5 supplies the signals they annotate.
- A-5 complements H-5/H-6: refusal shapes ↔ recheck/partial-failure outcomes.
- A-7 complements H-8: attribute growth to agents ↔ protect what agents hold.

### Opposition and Uncertainty
- **Recommended verb (A-2) vs no verdict register (H-3).** Type: true trade-off. Resolution candidate from D-9: the tool never says "safe"; it says *kind = build output, recovery = local rebuild, activity signals = {…}, matches grant G3*. The verb is a consequence of the human's grant plus the kind's recovery contract, not the tool's opinion. Decision: maintainer. Test: give an agent the fact-only output and see whether it plans soundly.
- **Archive above delete (H-4).** Type: evidence dispute. Decide by observing a real mid-flight ENOSPC on a multi-GB archive.
- **Trash-by-default vs freeing space.** Type: trade-off nobody named before. Deleting 8 GB to Trash frees nothing until emptied; snapshots may pin it after. If Trash is bypassed, that is a distinct verb with its own bar, not a flag.
- **GB-shaped outcome biases toward `delete`** (E). Type: evidence design creating safety pressure. `archive` and `prevent` never register on free space the day they're chosen. Needs a second outcome (re-ask rate, regrowth count) or they'll be starved.
- **Sink recheck cost (H-5) vs batch speed (E).** Type: evidence dispute. Measure proposal-to-execution window and divergence rate under real churn first.
- **Docker: extractor now (D-7/D-8) vs fact-only until a sink recheck exists (H).** Type: sequencing. Both hold: extract and attribute now, no destructive Docker verb until the daemon-side recheck is statable.

### Missing branches surfaced (nobody owned these before)
- The **generator** of an artifact (pnpm/npm, cargo, xcodebuild) has no entity; `prevent` therefore has no subject to record in the ledger.
- **Snapshots** may deserve to be entities with their own extractor, not a cause-of-residual.
- **Attribute the 47 GB once** — a one-command question with no recorded answer; every coverage claim is unsound until it has one.
- **Confidence decay** — a stale high-confidence signal is worse than a fresh low one.
- **Entity merge/split** once repo detection unifies path-keyed projects.
- **Silence on an authorization request** — neither approval nor refusal.
- **Tool-side circuit breaker** for a misbehaving agent (proposal rate, retries against refused units).
- **Agent reads its own history** — "this regrew 4× → propose prevent, not delete."
- **Regret rate** from Trash restores; **near-misses** caught at the sink — free safety signals currently discarded.

### Recommended Working Frame
Build an *entity graph with a consent ledger*, operated through a question-shaped agent surface, where the tool supplies facts with provenance and the human supplies grants; a verb is the intersection. Verify in free-space terms. Measure as an instrumented case study, not a trial.

### Alternate Frames Worth Keeping
- **Ranked list + `--json` is enough** (skeptic): if agents rank raw inventory themselves regardless, the MCP/question surface is overbuilt. A-2's review trigger decides.
- **Cube not graph**: artifact × project × worktree with cheap arithmetic; loses only if Docker's weak join or D-9's restore-source can't collapse to attributes.

### Evidence and Invalidation
- Need to learn: does pressure ever occur on the target machine; what the 47 GB is; divergence rate proposal→sink under churn; whether fact-only output yields sound agent plans; ledger growth over a month.
- Signal this synthesis is wrong: a month with zero floor crossings (premise gone); developers write one broad grant on day one (grant became the blank check); path-keyed view never re-asks after a rename (entity layer is overhead).

### Ready for Next Phase?
Yes for planning with dispositions kept `candidate` — the maintainer asked for discovery issues in the repo. One decision goes on the epic for the maintainer: A-2 vs H-3 with the D-9 resolution proposed.

### Next Route
`/oh-plan` → epic + child issues in `open-horizon-labs/slop-livin`, S&T lineage on each.

## Plan
Epic: https://github.com/open-horizon-labs/slop-livin/issues/1 — children #2–#15, each carrying S&T steps, sufficiency group, dependencies, acceptance signal. Dispositions remain `candidate` (discovery issues, per maintainer). Maintainer decision recorded on the epic: recommended verb (A-2) vs no verdict register (H-3), proposed resolution via recovery contract (D-9).

Build order implied by dependencies: #2 → #3 → {#4, #5, #8} → {#6, #7, #11, #12} → #9 → #10 → #13 → #14; #15 runs alongside.

## Language decision (2026-09-18)
Rust, not Go. Go was inherited from the Mole-contribution frame, which no longer applies. Salvage sources: fspulse `experiment/reverse-delta` (c561400b) for scanner + reverse-delta schema; repo-native-alignment for Extractor/Enricher/bus; mole `integrate` (Go) as the behavioral spec and test oracle — folding rules, hardlink attribution, coverage semantics, zstd levels, minutes-as-i32 mtime, ≥1 MB file rows, FSEvents refusal reasons, AST-style source audits (re-expressed with `syn`).
Renamed slop-nuke → slop-livin the same day.

## Salvage (2026-09-18 — project-oriented aim correction)

### Salvage Report

**Reason:** The first implementation and the first live run optimized for generic
volume cleanup and authorization, then reported volume totals and cleanup
categories when the requested product was project-oriented growth analysis.
The result was presented as complete even though `candidates` returned
`insufficient-evidence` and the scan of `/Users/muness1/src` returned zero
projects and zero artifacts while covering 32.67 GB.

**Original Aim (corrected):** A tool for developers who use many agents to
understand sudden explosive storage growth per Git project, per worktree, and
per artifact type. Every byte is attributed to a `.git`-identified checkout or
worktree, mapped to a project, or explicitly reported as unowned. Docker,
build targets, and dependency trees are sources of the same project-oriented
summary, not separate volume categories.

### Learnings

1. A scan root such as `/Users/muness1/src` is a container of projects, not a
   project. Project discovery must recurse for `.git` directories/files before
   summarization.
2. A project is identified by `.git` presence and has two forms: a main
   checkout or a linked worktree. Path is an observed attribute, not identity.
3. Artifact classification must happen at directory discovery time for
   `node_modules`, `target`, build/dist outputs, `.git`, and other dependency or
   generated trees. Waiting for an opaque/folded row caused the live run to
   report zero artifacts.
4. Docker facts are not useful as a separate total. Images, layers, build cache,
   and volumes must join to a project/worktree through explicit evidence; weak
   or absent joins must remain visible as `unowned`, never be guessed.
5. `du` totals and Docker totals are diagnostic evidence, not the product
   output. The primary output must be rows like:
   `project -> worktree -> artifact kind -> path -> bytes -> growth -> signals`.
6. A volume pressure question, synthetic `truth` command, or generic candidates
   list cannot substitute for a project growth explanation.

### Frame Shifts

`volume cleanup tool with project metadata` → `project/worktree/artifact growth
ledger with cleanup as a later consumer`.

`Docker is a separate accounting domain` → `Docker is an extractor whose facts
join to project entities or remain explicitly unowned`.

`artifact identity can be added after scanning` → `artifact classification and
project attribution are required at the scan boundary`.

### New Guardrails

1. No “project summary” is valid unless the input root recursively discovered
   at least one `.git` checkout/worktree or explicitly reports zero projects.
2. Every artifact row must carry `project_id`, `worktree_id` (when filesystem
   local), `kind`, `path`, `bytes`, `observed_at`, and attribution confidence.
3. A Docker object with no explicit project join is `owned_by_nothing`; name
   similarity alone cannot turn it into project-owned bytes.
4. Build outputs and dependency trees are attributed to the nearest containing
   checkout/worktree, not emitted as global totals.
5. Shared caches may have multiple project edges or no owner; never duplicate
   shared bytes into every project total.
6. The first acceptance test is a project-oriented report on a fixture with a
   main checkout, linked worktree, `node_modules`, `target`, and Docker objects.
   A green volume scan with no project rows is a failure.
7. Recommendations must be downstream of the report and must name the project,
   artifact kind, evidence, and recovery contract. Generic “prune Docker” is
   not an acceptable project recommendation.

### Missing Context

- The intended output shape was not made the first acceptance test.
- The live run happened before validating project discovery on a multi-repo
  root.
- Docker attribution evidence (Compose labels, image metadata, worktree paths)
  was not modeled into the summary path.
- Worktree fixtures and shared-cache fixtures were not present before the
  implementation was declared complete.

### Ownership / Coordination Breakdowns

The implementation treated issue boundaries and safety plumbing as the product
boundary. Review checked the code against the epic issues but did not force a
human-readable per-project output against the stated aim. The next review must
start with the report contract and a live multi-project fixture; safety and
authorization remain downstream constraints.

### Reusable Fragments

- Rust workspace and release/check pipeline.
- Real zstd-compressed Parquet writer keyed by volume.
- Git identity and nested repo-row propagation tests, subject to replacement by
  explicit checkout/worktree entities.
- Append-only action ledger and fail-closed grant/sink contracts, retained as a
  later action layer rather than the first product slice.
- Docker `system df` extraction as a raw fact source only.

### Fresh Start Recommendation

Restart from the report contract. Build fixture-backed project discovery first,
then artifact attribution and growth deltas, then Docker joins, then the
project-oriented CLI/MCP summary. Only after a useful summary exists should
grants, proposals, and destructive sinks be reintroduced. The first demo must
answer: “What grew, by project, worktree, and artifact type, since the previous
observation, and which bytes are unowned?”

## Plan (restart, 2026-09-18) — report contract first

Why the first plan failed: the product output was the acceptance signal of the *last* issue; every earlier issue could pass with zero projects discovered. Grants/ledger/execution were sequenced before anything a developer could read. "Docker is a separate domain" survived from the problem space after the model layer had already overturned it.

**Gate for everything:** `slop-livin report ~/src` on the real machine shows >0 projects, every byte either attributed to a checkout/worktree or listed as unowned, and totals reconcile to `du -skPx`. Until that is true no other issue may be worked.

| # | Slice | Done when |
|---|---|---|
| R1 | Report contract + fixture | Fixture: main checkout, linked worktree, `node_modules`, `target`, `dist`, a shared cache, mocked `docker system df -v`. Golden test asserting rows `project → worktree → kind → path → bytes → growth → signals` plus an `unowned` section. Red before any implementation. |
| R2 | Project discovery | Recurse root for `.git` dir/file; project = object-store id; checkout vs linked worktree; a root with zero projects says so explicitly. Live: 57 repos / 17 worktrees on `~/src`. |
| R3 | Artifact attribution at the scan boundary | Classify `node_modules`/`target`/`build`/`dist`/`.git`/… when the directory is *discovered*, attribute to nearest containing checkout/worktree; shared caches get multiple edges or `unowned`, never duplicated bytes. |
| R4 | Growth since previous observation | Reverse-delta store answers per-row `growth` and regrowth count; `--since` window is a setting. |
| R5 | Docker join by explicit evidence | Compose label / worktree path in metadata → project; else `owned_by_nothing`. Name similarity never attributes. |
| R6 | CLI + MCP summary | `slop-livin report [root] [--since]` and MCP `report` / `what_grew`. **Remove** `pressure`, `truth`, `candidates` from the surface. |
| R7 | Actions, downstream | Existing grants/ledger/execution/occupancy crates parked; reintroduced only as consumers of R6 rows, naming project, kind, evidence, recovery contract. |

Order is strict R1→R6; R7 not before a real developer has used R6 output. Fragments retained per Salvage: workspace + check pipeline, Parquet writer, git identity tests (to be replaced by explicit checkout/worktree entities), ledger/grant contracts (parked), Docker `system df` extraction as raw facts.
