//! history-sweeps-are-owned, coverage-changes-are-not-storage-changes,
//! reverse-delta-current-plus-deltas, dir-mtime-int32-minutes: stored
//! history rows, their tombstones, their schemas and the `Owned` claim a
//! tombstone needs live in a module private to `growth`.
use swamp_core::growth::columns::ArtifactHistory;

fn main() {}
