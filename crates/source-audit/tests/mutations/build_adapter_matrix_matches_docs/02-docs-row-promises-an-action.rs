//! target: docs/build-artifacts.md
//! mode: replace
//! why: a published table promising selective cleanup no adapter implements
# Build artifacts

| adapter | roles | actions |
|---|---|---|
| `cargo` | outputs, tests, dependencies | selective cleanup available |
| `node` | outputs, dependencies, caches | selective cleanup available |
| `gradle` | outputs, caches | selective cleanup available |
| `maven` | outputs, repository artifacts | selective cleanup available |
